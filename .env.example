# ORBIT environment variables — copy to .env.local and fill in.
# Everything is optional: without a key the contact form falls back to a
# mailto: handoff and logs submissions server-side (see SETUP.md).

# Resend (https://resend.com) — powers the contact form email delivery.
RESEND_API_KEY=
